@{
    Layout = "~/Views/Layout/AjaxSearch.cshtml";
}
@section ajaxUrl
{
    url : "/Magzines/GetJsonData",
}
@section checkBox
{
}
@section ajaxSendData
{
    var data = {
    str: $("#search_bar").val()
    }
}

@section ajaxRet{
    console.log(response);
    dataBlock.html("");
    for (var z in response) {
    //        debugger;
    var ndata = response[z];
    var ablock = nblock.clone();
    var blockData = ablock.find(".x_content .row");
    for (var x in ndata) {
    if (ndata.hasOwnProperty(x)) {
    var item = temp.clone();
    if (ndata[x].file == null || ndata[x].file.trim() == "") {

    item.find("img").attr("src", '@Url.Content("~/Content/data/book.png")');
    } else {
    item.find("img").attr("src", '@Url.Content("~/Content/data/Magzines/")' + ndata[x].file + ".jpg");
    }
    item.find(".caption p").html(ndata[x].name);

    item.find(".caption a").attr("href","/Magzines/Show/"+ndata[x].name);
    blockData.append(item);
    }
    }
    ablock.find(".x_title h2").text("Courses");
    dataBlock.append(ablock);
    }
}
Magzines